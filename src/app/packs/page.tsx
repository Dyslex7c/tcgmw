"use client";

import { PackOpeningStage } from "@/components/packs/PackOpeningStage";

export default function PacksPage() {
  return (
    <div className="max-w-[1400px] mx-auto px-4 sm:px-6 py-6">
      <PackOpeningStage />
    </div>
  );
}
